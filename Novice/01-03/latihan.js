function exampleScope() {
    var foo; // Declaration
    foo = document.write("hi"); // Initialisation
    
    var bar = document.write("hello"); // Declaration & Initialisation in one step
    }
    exampleScope();
        